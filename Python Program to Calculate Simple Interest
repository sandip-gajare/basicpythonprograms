principal =float(input("enter principal amount :"))
rate =float(input("enter rate of interest :"))
time =float(input("enter time in years :"))

simple_interest = (primcipal * rate * time) / 100
print("simple interest is:", simple_interest)
